<h2>編集画面</h2>
<%= form_with model: @list, url: update_list_path(@list.id), mothod: :patch do |f| %>
    <h4>タイトル</h4>
    <%= f.text_field :title %>
    <h4>中身</h4>
    <%= f.text_area :body %>
    <%= f.submit '更新' %>
<% end %>